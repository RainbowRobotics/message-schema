#include "middleware/session/session.h"

namespace rb::io {


  // Session Session::Open(const SessionOptions& options)
  // std::string Resource(std::string_view path) const;
  // void PublishBuf(std::string_view path, SessionBackend::Bytes buf);

}