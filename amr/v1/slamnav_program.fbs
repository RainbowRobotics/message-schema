
namespace SLAMNAV;

/*
    category    :       RPC
    response    :       One-shot
    topic       :       {robotType}/call_pause
    usage       :       로봇 동작 전체 일시 정지 ( movePause 동작 )
    field
        - result        :       동작 수행 결과 ( "success" / "fail" )
*/
table RequestCallPause {
}
table ResponseCallPause {
  result: string;
  message: string;
}


/*
    category    :       RPC
    response    :       One-shot
    topic       :       {robotType}/call_resume
    usage       :       로봇 동작 resume ( moveResume 동작 )
    field
        - result        :       동작 수행 결과 ( "success" / "fail" )
*/
table RequestCallResume {
}
table ResponseCallResume {
  result: string;
  message: string;
}


/*
    category    :       RPC
    response    :       One-shot
    topic       :       {robotType}/call_halt
    usage       :       로봇 동작 전체 일시 정지 ( moveStop 동작 )
    field
        - result        :       동작 수행 결과 ( "success" / "fail" )
*/
table RequestCallHalt {
}
table ResponseCallHalt {
  result: string;
  message: string;
}

/*
    category    :       RPC
    response    :       One-shot
    topic       :       {robotType}/call_whoami
    usage       :       로봇 정보 조회
    field
        - category    :       대분류 ...manipulate, mobile, sesnor
        - model       :       모델코드
        - version     :       4.7.1
        - alias       :       Ui표기용 string 별칭
*/
table RequestCallWhoami {
}
table ResponseCallWhoami {
    category    : string;
    model       : string;
    version     : string;
    alias       : string;
}

/*
    category    :       RPC
    response    :       One-shot
    topic       :       {robotType}/update
    usage       :       소프트웨어 업데이트
    field
        - branch        :       업데이트 브랜치
        - version       :       업데이트 버전
*/
table RequestUpdate {
    id: string;
    branch: string;
    version: string;
}
table ResponseUpdate {
    id: string;
    branch: string;
    version: string;
    result: string;
    message: string;
}

/*
    category    :       RPC
    response    :       One-shot
<<<<<<< HEAD
    topic       :       {robotType}/program/getVersion
=======
    topic       :       {robotType}/getVersion
>>>>>>> cf557d458676f1a736340e3fbe486d2274f4791f
    usage       :       현재 버전 조회
    field
        - version       :       현재 버전
        - branch        :       현재 브랜치
*/
table RequestCurrentVersion {
}
table ResponseCurrentVersion {
    version: string;        // BUILD_VERSION  (git describe --tags)
    git_commit  : string;   // BUILD_GIT_COMMIT
    git_branch  : string;   // BUILD_GIT_BRANCH
    git_author  : string;   // BUILD_GIT_AUTHOR
    build_date  : string;   // BUILD_DATE
    build_user  : string;   // BUILD_USER
    result: string;
    message: string;
}
