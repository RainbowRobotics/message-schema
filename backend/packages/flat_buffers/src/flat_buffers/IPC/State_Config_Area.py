# automatically generated by the FlatBuffers compiler, do not modify

# namespace: IPC

import flatbuffers
from flatbuffers.compat import import_numpy
from typing import Any
from flat_buffers.IPC.Vec3f import Vec3f, Vec3fT
from typing import Optional
np = import_numpy()

class State_Config_Area(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset: int = 0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = State_Config_Area()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsState_Config_Area(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # State_Config_Area
    def Init(self, buf: bytes, pos: int):
        self._tab = flatbuffers.table.Table(buf, pos)

    # State_Config_Area
    def AreaName(self) -> Optional[str]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # State_Config_Area
    def AreaType(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # State_Config_Area
    def AreaOffset(self) -> Optional[Vec3f]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = o + self._tab.Pos
            obj = Vec3f()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # State_Config_Area
    def AreaEuler(self) -> Optional[Vec3f]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = o + self._tab.Pos
            obj = Vec3f()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # State_Config_Area
    def AreaPara(self) -> Optional[Vec3f]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            x = o + self._tab.Pos
            obj = Vec3f()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def State_Config_AreaStart(builder: flatbuffers.Builder):
    builder.StartObject(5)

def Start(builder: flatbuffers.Builder):
    State_Config_AreaStart(builder)

def State_Config_AreaAddAreaName(builder: flatbuffers.Builder, areaName: int):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(areaName), 0)

def AddAreaName(builder: flatbuffers.Builder, areaName: int):
    State_Config_AreaAddAreaName(builder, areaName)

def State_Config_AreaAddAreaType(builder: flatbuffers.Builder, areaType: int):
    builder.PrependInt32Slot(1, areaType, 0)

def AddAreaType(builder: flatbuffers.Builder, areaType: int):
    State_Config_AreaAddAreaType(builder, areaType)

def State_Config_AreaAddAreaOffset(builder: flatbuffers.Builder, areaOffset: Any):
    builder.PrependStructSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(areaOffset), 0)

def AddAreaOffset(builder: flatbuffers.Builder, areaOffset: Any):
    State_Config_AreaAddAreaOffset(builder, areaOffset)

def State_Config_AreaAddAreaEuler(builder: flatbuffers.Builder, areaEuler: Any):
    builder.PrependStructSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(areaEuler), 0)

def AddAreaEuler(builder: flatbuffers.Builder, areaEuler: Any):
    State_Config_AreaAddAreaEuler(builder, areaEuler)

def State_Config_AreaAddAreaPara(builder: flatbuffers.Builder, areaPara: Any):
    builder.PrependStructSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(areaPara), 0)

def AddAreaPara(builder: flatbuffers.Builder, areaPara: Any):
    State_Config_AreaAddAreaPara(builder, areaPara)

def State_Config_AreaEnd(builder: flatbuffers.Builder) -> int:
    return builder.EndObject()

def End(builder: flatbuffers.Builder) -> int:
    return State_Config_AreaEnd(builder)

try:
    from typing import Optional
except:
    pass

class State_Config_AreaT(object):

    # State_Config_AreaT
    def __init__(self):
        self.areaName = None  # type: str
        self.areaType = 0  # type: int
        self.areaOffset = None  # type: Optional[Vec3fT]
        self.areaEuler = None  # type: Optional[Vec3fT]
        self.areaPara = None  # type: Optional[Vec3fT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        stateConfigArea = State_Config_Area()
        stateConfigArea.Init(buf, pos)
        return cls.InitFromObj(stateConfigArea)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, stateConfigArea):
        x = State_Config_AreaT()
        x._UnPack(stateConfigArea)
        return x

    # State_Config_AreaT
    def _UnPack(self, stateConfigArea):
        if stateConfigArea is None:
            return
        self.areaName = stateConfigArea.AreaName()
        self.areaType = stateConfigArea.AreaType()
        if stateConfigArea.AreaOffset() is not None:
            self.areaOffset = Vec3fT.InitFromObj(stateConfigArea.AreaOffset())
        if stateConfigArea.AreaEuler() is not None:
            self.areaEuler = Vec3fT.InitFromObj(stateConfigArea.AreaEuler())
        if stateConfigArea.AreaPara() is not None:
            self.areaPara = Vec3fT.InitFromObj(stateConfigArea.AreaPara())

    # State_Config_AreaT
    def Pack(self, builder):
        if self.areaName is not None:
            areaName = builder.CreateString(self.areaName)
        State_Config_AreaStart(builder)
        if self.areaName is not None:
            State_Config_AreaAddAreaName(builder, areaName)
        State_Config_AreaAddAreaType(builder, self.areaType)
        if self.areaOffset is not None:
            areaOffset = self.areaOffset.Pack(builder)
            State_Config_AreaAddAreaOffset(builder, areaOffset)
        if self.areaEuler is not None:
            areaEuler = self.areaEuler.Pack(builder)
            State_Config_AreaAddAreaEuler(builder, areaEuler)
        if self.areaPara is not None:
            areaPara = self.areaPara.Pack(builder)
            State_Config_AreaAddAreaPara(builder, areaPara)
        stateConfigArea = State_Config_AreaEnd(builder)
        return stateConfigArea
