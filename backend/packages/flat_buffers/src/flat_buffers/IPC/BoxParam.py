# automatically generated by the FlatBuffers compiler, do not modify

# namespace: IPC

import flatbuffers
from flatbuffers.compat import import_numpy
from typing import Any
np = import_numpy()

class BoxParam(object):
    __slots__ = ['_tab']

    @classmethod
    def SizeOf(cls) -> int:
        return 36

    # BoxParam
    def Init(self, buf: bytes, pos: int):
        self._tab = flatbuffers.table.Table(buf, pos)

    # BoxParam
    def F(self, j = None):
        if j is None:
            return [self._tab.Get(flatbuffers.number_types.Float32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(0 + i * 4)) for i in range(self.FLength())]
        elif j >= 0 and j < self.FLength():
            return self._tab.Get(flatbuffers.number_types.Float32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(0 + j * 4))
        else:
            return None

    # BoxParam
    def FAsNumpy(self):
        return self._tab.GetArrayAsNumpy(flatbuffers.number_types.Float32Flags, self._tab.Pos + 0, self.FLength())

    # BoxParam
    def FLength(self) -> int:
        return 9

    # BoxParam
    def FIsNone(self) -> bool:
        return False


def CreateBoxParam(builder, f):
    builder.Prep(4, 36)
    for _idx0 in range(9 , 0, -1):
        builder.PrependFloat32(f[_idx0-1])
    return builder.Offset()

try:
    from typing import List
except:
    pass

class BoxParamT(object):

    # BoxParamT
    def __init__(self):
        self.f = None  # type: List[float]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        boxParam = BoxParam()
        boxParam.Init(buf, pos)
        return cls.InitFromObj(boxParam)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, boxParam):
        x = BoxParamT()
        x._UnPack(boxParam)
        return x

    # BoxParamT
    def _UnPack(self, boxParam):
        if boxParam is None:
            return
        if not boxParam.FIsNone():
            if np is None:
                self.f = []
                for i in range(boxParam.FLength()):
                    self.f.append(boxParam.F(i))
            else:
                self.f = boxParam.FAsNumpy()

    # BoxParamT
    def Pack(self, builder):
        return CreateBoxParam(builder, self.f)
