# automatically generated by the FlatBuffers compiler, do not modify

# namespace: v1

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class State(object):
    __slots__ = ['_tab']

    @classmethod
    def SizeOf(cls):
        return 56

    # State
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # State
    def Q(self, j = None):
        if j is None:
            return [self._tab.Get(flatbuffers.number_types.Float32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(0 + i * 4)) for i in range(self.QLength())]
        elif j >= 0 and j < self.QLength():
            return self._tab.Get(flatbuffers.number_types.Float32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(0 + j * 4))
        else:
            return None

    # State
    def QAsNumpy(self):
        return self._tab.GetArrayAsNumpy(flatbuffers.number_types.Float32Flags, self._tab.Pos + 0, self.QLength())

    # State
    def QLength(self):
        return 7

    # State
    def QIsNone(self):
        return False

    # State
    def Qdot(self, j = None):
        if j is None:
            return [self._tab.Get(flatbuffers.number_types.Float32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(28 + i * 4)) for i in range(self.QdotLength())]
        elif j >= 0 and j < self.QdotLength():
            return self._tab.Get(flatbuffers.number_types.Float32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(28 + j * 4))
        else:
            return None

    # State
    def QdotAsNumpy(self):
        return self._tab.GetArrayAsNumpy(flatbuffers.number_types.Float32Flags, self._tab.Pos + 28, self.QdotLength())

    # State
    def QdotLength(self):
        return 7

    # State
    def QdotIsNone(self):
        return False


def CreateState(builder, q, qdot):
    builder.Prep(4, 56)
    for _idx0 in range(7 , 0, -1):
        builder.PrependFloat32(qdot[_idx0-1])
    for _idx0 in range(7 , 0, -1):
        builder.PrependFloat32(q[_idx0-1])
    return builder.Offset()

try:
    from typing import List
except:
    pass

class StateT(object):

    # StateT
    def __init__(self):
        self.q = None  # type: List[float]
        self.qdot = None  # type: List[float]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        state = State()
        state.Init(buf, pos)
        return cls.InitFromObj(state)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, state):
        x = StateT()
        x._UnPack(state)
        return x

    # StateT
    def _UnPack(self, state):
        if state is None:
            return
        if not state.QIsNone():
            if np is None:
                self.q = []
                for i in range(state.QLength()):
                    self.q.append(state.Q(i))
            else:
                self.q = state.QAsNumpy()
        if not state.QdotIsNone():
            if np is None:
                self.qdot = []
                for i in range(state.QdotLength()):
                    self.qdot.append(state.Qdot(i))
            else:
                self.qdot = state.QdotAsNumpy()

    # StateT
    def Pack(self, builder):
        return CreateState(builder, self.q, self.qdot)
