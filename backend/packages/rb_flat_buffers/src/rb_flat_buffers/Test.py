# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
from typing import Any
np = import_numpy()

class Test(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset: int = 0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = Test()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTest(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # Test
    def Init(self, buf: bytes, pos: int):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Test
    def Test(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 0.0

    # Test
    def Test2(self, j: int):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # Test
    def Test2AsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float32Flags, o)
        return 0

    # Test
    def Test2Length(self) -> int:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # Test
    def Test2IsNone(self) -> bool:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

def TestStart(builder: flatbuffers.Builder):
    builder.StartObject(2)

def Start(builder: flatbuffers.Builder):
    TestStart(builder)

def TestAddTest(builder: flatbuffers.Builder, test: float):
    builder.PrependFloat32Slot(0, test, 0.0)

def AddTest(builder: flatbuffers.Builder, test: float):
    TestAddTest(builder, test)

def TestAddTest2(builder: flatbuffers.Builder, test2: int):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(test2), 0)

def AddTest2(builder: flatbuffers.Builder, test2: int):
    TestAddTest2(builder, test2)

def TestStartTest2Vector(builder, numElems: int) -> int:
    return builder.StartVector(4, numElems, 4)

def StartTest2Vector(builder, numElems: int) -> int:
    return TestStartTest2Vector(builder, numElems)

def TestEnd(builder: flatbuffers.Builder) -> int:
    return builder.EndObject()

def End(builder: flatbuffers.Builder) -> int:
    return TestEnd(builder)

try:
    from typing import List
except:
    pass

class TestT(object):

    # TestT
    def __init__(
        self,
        test = 0.0,
        test2 = None,
    ):
        self.test = test  # type: float
        self.test2 = test2  # type: Optional[List[float]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        test = Test()
        test.Init(buf, pos)
        return cls.InitFromObj(test)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, test):
        x = TestT()
        x._UnPack(test)
        return x

    # TestT
    def _UnPack(self, test):
        if test is None:
            return
        self.test = test.Test()
        if not test.Test2IsNone():
            if np is None:
                self.test2 = []
                for i in range(test.Test2Length()):
                    self.test2.append(test.Test2(i))
            else:
                self.test2 = test.Test2AsNumpy()

    # TestT
    def Pack(self, builder):
        if self.test2 is not None:
            if np is not None and type(self.test2) is np.ndarray:
                test2 = builder.CreateNumpyVector(self.test2)
            else:
                TestStartTest2Vector(builder, len(self.test2))
                for i in reversed(range(len(self.test2))):
                    builder.PrependFloat32(self.test2[i])
                test2 = builder.EndVector()
        TestStart(builder)
        TestAddTest(builder, self.test)
        if self.test2 is not None:
            TestAddTest2(builder, test2)
        test = TestEnd(builder)
        return test
