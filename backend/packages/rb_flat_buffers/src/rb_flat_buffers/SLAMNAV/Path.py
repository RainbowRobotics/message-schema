# automatically generated by the FlatBuffers compiler, do not modify

# namespace: SLAMNAV

import flatbuffers
from flatbuffers.compat import import_numpy
from typing import Any
from SLAMNAV.PointArray import PointArray
from typing import Optional
np = import_numpy()

class Path(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset: int = 0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = Path()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPath(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # Path
    def Init(self, buf: bytes, pos: int):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Path
    def Value(self, j: int) -> Optional[PointArray]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 24
            obj = PointArray()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Path
    def ValueLength(self) -> int:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # Path
    def ValueIsNone(self) -> bool:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def PathStart(builder: flatbuffers.Builder):
    builder.StartObject(1)

def Start(builder: flatbuffers.Builder):
    PathStart(builder)

def PathAddValue(builder: flatbuffers.Builder, value: int):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(value), 0)

def AddValue(builder: flatbuffers.Builder, value: int):
    PathAddValue(builder, value)

def PathStartValueVector(builder, numElems: int) -> int:
    return builder.StartVector(24, numElems, 8)

def StartValueVector(builder, numElems: int) -> int:
    return PathStartValueVector(builder, numElems)

def PathEnd(builder: flatbuffers.Builder) -> int:
    return builder.EndObject()

def End(builder: flatbuffers.Builder) -> int:
    return PathEnd(builder)

try:
    from typing import List
except:
    pass

class PathT(object):

    # PathT
    def __init__(
        self,
        value = None,
    ):
        self.value = value  # type: Optional[List[PointArrayT]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        path = Path()
        path.Init(buf, pos)
        return cls.InitFromObj(path)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, path):
        x = PathT()
        x._UnPack(path)
        return x

    # PathT
    def _UnPack(self, path):
        if path is None:
            return
        if not path.ValueIsNone():
            self.value = []
            for i in range(path.ValueLength()):
                if path.Value(i) is None:
                    self.value.append(None)
                else:
                    pointArray_ = PointArrayT.InitFromObj(path.Value(i))
                    self.value.append(pointArray_)

    # PathT
    def Pack(self, builder):
        if self.value is not None:
            PathStartValueVector(builder, len(self.value))
            for i in reversed(range(len(self.value))):
                self.value[i].Pack(builder)
            value = builder.EndVector()
        PathStart(builder)
        if self.value is not None:
            PathAddValue(builder, value)
        path = PathEnd(builder)
        return path
