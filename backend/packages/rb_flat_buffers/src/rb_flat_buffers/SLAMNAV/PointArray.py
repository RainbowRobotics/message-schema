# automatically generated by the FlatBuffers compiler, do not modify

# namespace: SLAMNAV

import flatbuffers
from flatbuffers.compat import import_numpy
from typing import Any
np = import_numpy()

class PointArray(object):
    __slots__ = ['_tab']

    @classmethod
    def SizeOf(cls) -> int:
        return 24

    # PointArray
    def Init(self, buf: bytes, pos: int):
        self._tab = flatbuffers.table.Table(buf, pos)

    # PointArray
    def Point(self, j = None):
        if j is None:
            return [self._tab.Get(flatbuffers.number_types.Float64Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(0 + i * 8)) for i in range(self.PointLength())]
        elif j >= 0 and j < self.PointLength():
            return self._tab.Get(flatbuffers.number_types.Float64Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(0 + j * 8))
        else:
            return None

    # PointArray
    def PointAsNumpy(self):
        return self._tab.GetArrayAsNumpy(flatbuffers.number_types.Float64Flags, self._tab.Pos + 0, self.PointLength())

    # PointArray
    def PointLength(self) -> int:
        return 3

    # PointArray
    def PointIsNone(self) -> bool:
        return False


def CreatePointArray(builder, point):
    builder.Prep(8, 24)
    for _idx0 in range(3 , 0, -1):
        builder.PrependFloat64(point[_idx0-1])
    return builder.Offset()

try:
    from typing import List
except:
    pass

class PointArrayT(object):

    # PointArrayT
    def __init__(
        self,
        point = None,
    ):
        self.point = point  # type: Optional[List[float]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        pointArray = PointArray()
        pointArray.Init(buf, pos)
        return cls.InitFromObj(pointArray)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, pointArray):
        x = PointArrayT()
        x._UnPack(pointArray)
        return x

    # PointArrayT
    def _UnPack(self, pointArray):
        if pointArray is None:
            return
        if not pointArray.PointIsNone():
            if np is None:
                self.point = []
                for i in range(pointArray.PointLength()):
                    self.point.append(pointArray.Point(i))
            else:
                self.point = pointArray.PointAsNumpy()

    # PointArrayT
    def Pack(self, builder):
        return CreatePointArray(builder, self.point)
