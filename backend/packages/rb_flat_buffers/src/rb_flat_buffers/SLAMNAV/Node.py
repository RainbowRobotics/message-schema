# automatically generated by the FlatBuffers compiler, do not modify

# namespace: SLAMNAV

import flatbuffers
from flatbuffers.compat import import_numpy
from typing import Any
from typing import Optional
np = import_numpy()

class Node(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset: int = 0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = Node()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsNode(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # Node
    def Init(self, buf: bytes, pos: int):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Node
    def Id(self) -> Optional[str]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Node
    def Name(self) -> Optional[str]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Node
    def Pose(self, j: int):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # Node
    def PoseAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # Node
    def PoseLength(self) -> int:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # Node
    def PoseIsNone(self) -> bool:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # Node
    def Info(self) -> Optional[str]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Node
    def Links(self, j: int):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # Node
    def LinksLength(self) -> int:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # Node
    def LinksIsNone(self) -> bool:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

    # Node
    def Type(self) -> Optional[str]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def NodeStart(builder: flatbuffers.Builder):
    builder.StartObject(6)

def Start(builder: flatbuffers.Builder):
    NodeStart(builder)

def NodeAddId(builder: flatbuffers.Builder, id: int):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(id), 0)

def AddId(builder: flatbuffers.Builder, id: int):
    NodeAddId(builder, id)

def NodeAddName(builder: flatbuffers.Builder, name: int):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(name), 0)

def AddName(builder: flatbuffers.Builder, name: int):
    NodeAddName(builder, name)

def NodeAddPose(builder: flatbuffers.Builder, pose: int):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(pose), 0)

def AddPose(builder: flatbuffers.Builder, pose: int):
    NodeAddPose(builder, pose)

def NodeStartPoseVector(builder, numElems: int) -> int:
    return builder.StartVector(8, numElems, 8)

def StartPoseVector(builder, numElems: int) -> int:
    return NodeStartPoseVector(builder, numElems)

def NodeAddInfo(builder: flatbuffers.Builder, info: int):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(info), 0)

def AddInfo(builder: flatbuffers.Builder, info: int):
    NodeAddInfo(builder, info)

def NodeAddLinks(builder: flatbuffers.Builder, links: int):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(links), 0)

def AddLinks(builder: flatbuffers.Builder, links: int):
    NodeAddLinks(builder, links)

def NodeStartLinksVector(builder, numElems: int) -> int:
    return builder.StartVector(4, numElems, 4)

def StartLinksVector(builder, numElems: int) -> int:
    return NodeStartLinksVector(builder, numElems)

def NodeAddType(builder: flatbuffers.Builder, type: int):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(type), 0)

def AddType(builder: flatbuffers.Builder, type: int):
    NodeAddType(builder, type)

def NodeEnd(builder: flatbuffers.Builder) -> int:
    return builder.EndObject()

def End(builder: flatbuffers.Builder) -> int:
    return NodeEnd(builder)

try:
    from typing import List
except:
    pass

class NodeT(object):

    # NodeT
    def __init__(self):
        self.id = None  # type: str
        self.name = None  # type: str
        self.pose = None  # type: List[float]
        self.info = None  # type: str
        self.links = None  # type: List[str]
        self.type = None  # type: str

    @classmethod
    def InitFromBuf(cls, buf, pos):
        node = Node()
        node.Init(buf, pos)
        return cls.InitFromObj(node)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, node):
        x = NodeT()
        x._UnPack(node)
        return x

    # NodeT
    def _UnPack(self, node):
        if node is None:
            return
        self.id = node.Id()
        self.name = node.Name()
        if not node.PoseIsNone():
            if np is None:
                self.pose = []
                for i in range(node.PoseLength()):
                    self.pose.append(node.Pose(i))
            else:
                self.pose = node.PoseAsNumpy()
        self.info = node.Info()
        if not node.LinksIsNone():
            self.links = []
            for i in range(node.LinksLength()):
                self.links.append(node.Links(i))
        self.type = node.Type()

    # NodeT
    def Pack(self, builder):
        if self.id is not None:
            id = builder.CreateString(self.id)
        if self.name is not None:
            name = builder.CreateString(self.name)
        if self.pose is not None:
            if np is not None and type(self.pose) is np.ndarray:
                pose = builder.CreateNumpyVector(self.pose)
            else:
                NodeStartPoseVector(builder, len(self.pose))
                for i in reversed(range(len(self.pose))):
                    builder.PrependFloat64(self.pose[i])
                pose = builder.EndVector()
        if self.info is not None:
            info = builder.CreateString(self.info)
        if self.links is not None:
            linkslist = []
            for i in range(len(self.links)):
                linkslist.append(builder.CreateString(self.links[i]))
            NodeStartLinksVector(builder, len(self.links))
            for i in reversed(range(len(self.links))):
                builder.PrependUOffsetTRelative(linkslist[i])
            links = builder.EndVector()
        if self.type is not None:
            type = builder.CreateString(self.type)
        NodeStart(builder)
        if self.id is not None:
            NodeAddId(builder, id)
        if self.name is not None:
            NodeAddName(builder, name)
        if self.pose is not None:
            NodeAddPose(builder, pose)
        if self.info is not None:
            NodeAddInfo(builder, info)
        if self.links is not None:
            NodeAddLinks(builder, links)
        if self.type is not None:
            NodeAddType(builder, type)
        node = NodeEnd(builder)
        return node
