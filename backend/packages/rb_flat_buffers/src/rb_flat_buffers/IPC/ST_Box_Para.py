# automatically generated by the FlatBuffers compiler, do not modify

# namespace: IPC

import flatbuffers
from flatbuffers.compat import import_numpy
from typing import Any
np = import_numpy()

class ST_Box_Para(object):
    __slots__ = ['_tab']

    @classmethod
    def SizeOf(cls) -> int:
        return 36

    # ST_Box_Para
    def Init(self, buf: bytes, pos: int):
        self._tab = flatbuffers.table.Table(buf, pos)

    # ST_Box_Para
    def F(self, j = None):
        if j is None:
            return [self._tab.Get(flatbuffers.number_types.Float32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(0 + i * 4)) for i in range(self.FLength())]
        elif j >= 0 and j < self.FLength():
            return self._tab.Get(flatbuffers.number_types.Float32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(0 + j * 4))
        else:
            return None

    # ST_Box_Para
    def FAsNumpy(self):
        return self._tab.GetArrayAsNumpy(flatbuffers.number_types.Float32Flags, self._tab.Pos + 0, self.FLength())

    # ST_Box_Para
    def FLength(self) -> int:
        return 9

    # ST_Box_Para
    def FIsNone(self) -> bool:
        return False


def CreateST_Box_Para(builder, f):
    builder.Prep(4, 36)
    for _idx0 in range(9 , 0, -1):
        builder.PrependFloat32(f[_idx0-1])
    return builder.Offset()

try:
    from typing import List
except:
    pass

class ST_Box_ParaT(object):

    # ST_Box_ParaT
    def __init__(
        self,
        f = None,
    ):
        self.f = f  # type: Optional[List[float]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        stBoxPara = ST_Box_Para()
        stBoxPara.Init(buf, pos)
        return cls.InitFromObj(stBoxPara)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, stBoxPara):
        x = ST_Box_ParaT()
        x._UnPack(stBoxPara)
        return x

    # ST_Box_ParaT
    def _UnPack(self, stBoxPara):
        if stBoxPara is None:
            return
        if not stBoxPara.FIsNone():
            if np is None:
                self.f = []
                for i in range(stBoxPara.FLength()):
                    self.f.append(stBoxPara.F(i))
            else:
                self.f = stBoxPara.FAsNumpy()

    # ST_Box_ParaT
    def Pack(self, builder):
        return CreateStBoxPara(builder, self.f)
