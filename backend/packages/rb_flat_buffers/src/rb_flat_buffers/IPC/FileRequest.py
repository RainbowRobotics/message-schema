# automatically generated by the FlatBuffers compiler, do not modify

# namespace: IPC

import flatbuffers
from flatbuffers.compat import import_numpy
from typing import Any
from typing import Optional
np = import_numpy()

class FileRequest(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset: int = 0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = FileRequest()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsFileRequest(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # FileRequest
    def Init(self, buf: bytes, pos: int):
        self._tab = flatbuffers.table.Table(buf, pos)

    # FileRequest
    def Session(self) -> Optional[str]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # FileRequest
    def WantIndex(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

def FileRequestStart(builder: flatbuffers.Builder):
    builder.StartObject(2)

def Start(builder: flatbuffers.Builder):
    FileRequestStart(builder)

def FileRequestAddSession(builder: flatbuffers.Builder, session: int):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(session), 0)

def AddSession(builder: flatbuffers.Builder, session: int):
    FileRequestAddSession(builder, session)

def FileRequestAddWantIndex(builder: flatbuffers.Builder, wantIndex: int):
    builder.PrependUint32Slot(1, wantIndex, 0)

def AddWantIndex(builder: flatbuffers.Builder, wantIndex: int):
    FileRequestAddWantIndex(builder, wantIndex)

def FileRequestEnd(builder: flatbuffers.Builder) -> int:
    return builder.EndObject()

def End(builder: flatbuffers.Builder) -> int:
    return FileRequestEnd(builder)


class FileRequestT(object):

    # FileRequestT
    def __init__(self):
        self.session = None  # type: str
        self.wantIndex = 0  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        fileRequest = FileRequest()
        fileRequest.Init(buf, pos)
        return cls.InitFromObj(fileRequest)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, fileRequest):
        x = FileRequestT()
        x._UnPack(fileRequest)
        return x

    # FileRequestT
    def _UnPack(self, fileRequest):
        if fileRequest is None:
            return
        self.session = fileRequest.Session()
        self.wantIndex = fileRequest.WantIndex()

    # FileRequestT
    def Pack(self, builder):
        if self.session is not None:
            session = builder.CreateString(self.session)
        FileRequestStart(builder)
        if self.session is not None:
            FileRequestAddSession(builder, session)
        FileRequestAddWantIndex(builder, self.wantIndex)
        fileRequest = FileRequestEnd(builder)
        return fileRequest
