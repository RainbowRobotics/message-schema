# automatically generated by the FlatBuffers compiler, do not modify

# namespace: IPC

import flatbuffers
from flatbuffers.compat import import_numpy
from typing import Any
from typing import Optional
np = import_numpy()

class FileChunk(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset: int = 0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = FileChunk()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsFileChunk(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # FileChunk
    def Init(self, buf: bytes, pos: int):
        self._tab = flatbuffers.table.Table(buf, pos)

    # FileChunk
    def Session(self) -> Optional[str]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # FileChunk
    def Index(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # FileChunk
    def Offset(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # FileChunk
    def Data(self, j: int):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # FileChunk
    def DataAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # FileChunk
    def DataLength(self) -> int:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # FileChunk
    def DataIsNone(self) -> bool:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        return o == 0

def FileChunkStart(builder: flatbuffers.Builder):
    builder.StartObject(4)

def Start(builder: flatbuffers.Builder):
    FileChunkStart(builder)

def FileChunkAddSession(builder: flatbuffers.Builder, session: int):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(session), 0)

def AddSession(builder: flatbuffers.Builder, session: int):
    FileChunkAddSession(builder, session)

def FileChunkAddIndex(builder: flatbuffers.Builder, index: int):
    builder.PrependUint32Slot(1, index, 0)

def AddIndex(builder: flatbuffers.Builder, index: int):
    FileChunkAddIndex(builder, index)

def FileChunkAddOffset(builder: flatbuffers.Builder, offset: int):
    builder.PrependUint64Slot(2, offset, 0)

def AddOffset(builder: flatbuffers.Builder, offset: int):
    FileChunkAddOffset(builder, offset)

def FileChunkAddData(builder: flatbuffers.Builder, data: int):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(data), 0)

def AddData(builder: flatbuffers.Builder, data: int):
    FileChunkAddData(builder, data)

def FileChunkStartDataVector(builder, numElems: int) -> int:
    return builder.StartVector(1, numElems, 1)

def StartDataVector(builder, numElems: int) -> int:
    return FileChunkStartDataVector(builder, numElems)

def FileChunkEnd(builder: flatbuffers.Builder) -> int:
    return builder.EndObject()

def End(builder: flatbuffers.Builder) -> int:
    return FileChunkEnd(builder)

try:
    from typing import List
except:
    pass

class FileChunkT(object):

    # FileChunkT
    def __init__(
        self,
        session = None,
        index = 0,
        offset = 0,
        data = None,
    ):
        self.session = session  # type: Optional[str]
        self.index = index  # type: int
        self.offset = offset  # type: int
        self.data = data  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        fileChunk = FileChunk()
        fileChunk.Init(buf, pos)
        return cls.InitFromObj(fileChunk)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, fileChunk):
        x = FileChunkT()
        x._UnPack(fileChunk)
        return x

    # FileChunkT
    def _UnPack(self, fileChunk):
        if fileChunk is None:
            return
        self.session = fileChunk.Session()
        self.index = fileChunk.Index()
        self.offset = fileChunk.Offset()
        if not fileChunk.DataIsNone():
            if np is None:
                self.data = []
                for i in range(fileChunk.DataLength()):
                    self.data.append(fileChunk.Data(i))
            else:
                self.data = fileChunk.DataAsNumpy()

    # FileChunkT
    def Pack(self, builder):
        if self.session is not None:
            session = builder.CreateString(self.session)
        if self.data is not None:
            if np is not None and type(self.data) is np.ndarray:
                data = builder.CreateNumpyVector(self.data)
            else:
                FileChunkStartDataVector(builder, len(self.data))
                for i in reversed(range(len(self.data))):
                    builder.PrependUint8(self.data[i])
                data = builder.EndVector()
        FileChunkStart(builder)
        if self.session is not None:
            FileChunkAddSession(builder, session)
        FileChunkAddIndex(builder, self.index)
        FileChunkAddOffset(builder, self.offset)
        if self.data is not None:
            FileChunkAddData(builder, data)
        fileChunk = FileChunkEnd(builder)
        return fileChunk
