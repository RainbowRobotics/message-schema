# automatically generated by the FlatBuffers compiler, do not modify

# namespace: IPC

import flatbuffers
from flatbuffers.compat import import_numpy
from typing import Any
from IPC.Vec3f import Vec3f
from typing import Optional
np = import_numpy()

class ST_Config_Area(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset: int = 0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = ST_Config_Area()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsST_Config_Area(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # ST_Config_Area
    def Init(self, buf: bytes, pos: int):
        self._tab = flatbuffers.table.Table(buf, pos)

    # ST_Config_Area
    def AreaName(self) -> Optional[str]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # ST_Config_Area
    def AreaType(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # ST_Config_Area
    def AreaOffset(self) -> Optional[Vec3f]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = o + self._tab.Pos
            obj = Vec3f()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # ST_Config_Area
    def AreaEuler(self) -> Optional[Vec3f]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = o + self._tab.Pos
            obj = Vec3f()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # ST_Config_Area
    def AreaPara(self) -> Optional[Vec3f]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            x = o + self._tab.Pos
            obj = Vec3f()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def ST_Config_AreaStart(builder: flatbuffers.Builder):
    builder.StartObject(5)

def Start(builder: flatbuffers.Builder):
    ST_Config_AreaStart(builder)

def ST_Config_AreaAddAreaName(builder: flatbuffers.Builder, areaName: int):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(areaName), 0)

def AddAreaName(builder: flatbuffers.Builder, areaName: int):
    ST_Config_AreaAddAreaName(builder, areaName)

def ST_Config_AreaAddAreaType(builder: flatbuffers.Builder, areaType: int):
    builder.PrependInt32Slot(1, areaType, 0)

def AddAreaType(builder: flatbuffers.Builder, areaType: int):
    ST_Config_AreaAddAreaType(builder, areaType)

def ST_Config_AreaAddAreaOffset(builder: flatbuffers.Builder, areaOffset: Any):
    builder.PrependStructSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(areaOffset), 0)

def AddAreaOffset(builder: flatbuffers.Builder, areaOffset: Any):
    ST_Config_AreaAddAreaOffset(builder, areaOffset)

def ST_Config_AreaAddAreaEuler(builder: flatbuffers.Builder, areaEuler: Any):
    builder.PrependStructSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(areaEuler), 0)

def AddAreaEuler(builder: flatbuffers.Builder, areaEuler: Any):
    ST_Config_AreaAddAreaEuler(builder, areaEuler)

def ST_Config_AreaAddAreaPara(builder: flatbuffers.Builder, areaPara: Any):
    builder.PrependStructSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(areaPara), 0)

def AddAreaPara(builder: flatbuffers.Builder, areaPara: Any):
    ST_Config_AreaAddAreaPara(builder, areaPara)

def ST_Config_AreaEnd(builder: flatbuffers.Builder) -> int:
    return builder.EndObject()

def End(builder: flatbuffers.Builder) -> int:
    return ST_Config_AreaEnd(builder)

try:
    from typing import Optional
except:
    pass

class ST_Config_AreaT(object):

    # ST_Config_AreaT
    def __init__(
        self,
        areaName = None,
        areaType = 0,
        areaOffset = None,
        areaEuler = None,
        areaPara = None,
    ):
        self.areaName = areaName  # type: Optional[str]
        self.areaType = areaType  # type: int
        self.areaOffset = areaOffset  # type: Optional[Vec3fT]
        self.areaEuler = areaEuler  # type: Optional[Vec3fT]
        self.areaPara = areaPara  # type: Optional[Vec3fT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        stConfigArea = ST_Config_Area()
        stConfigArea.Init(buf, pos)
        return cls.InitFromObj(stConfigArea)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, stConfigArea):
        x = ST_Config_AreaT()
        x._UnPack(stConfigArea)
        return x

    # ST_Config_AreaT
    def _UnPack(self, stConfigArea):
        if stConfigArea is None:
            return
        self.areaName = stConfigArea.AreaName()
        self.areaType = stConfigArea.AreaType()
        if stConfigArea.AreaOffset() is not None:
            self.areaOffset = Vec3fT.InitFromObj(stConfigArea.AreaOffset())
        if stConfigArea.AreaEuler() is not None:
            self.areaEuler = Vec3fT.InitFromObj(stConfigArea.AreaEuler())
        if stConfigArea.AreaPara() is not None:
            self.areaPara = Vec3fT.InitFromObj(stConfigArea.AreaPara())

    # ST_Config_AreaT
    def Pack(self, builder):
        if self.areaName is not None:
            areaName = builder.CreateString(self.areaName)
        ST_Config_AreaStart(builder)
        if self.areaName is not None:
            ST_Config_AreaAddAreaName(builder, areaName)
        ST_Config_AreaAddAreaType(builder, self.areaType)
        if self.areaOffset is not None:
            areaOffset = self.areaOffset.Pack(builder)
            ST_Config_AreaAddAreaOffset(builder, areaOffset)
        if self.areaEuler is not None:
            areaEuler = self.areaEuler.Pack(builder)
            ST_Config_AreaAddAreaEuler(builder, areaEuler)
        if self.areaPara is not None:
            areaPara = self.areaPara.Pack(builder)
            ST_Config_AreaAddAreaPara(builder, areaPara)
        stConfigArea = ST_Config_AreaEnd(builder)
        return stConfigArea
