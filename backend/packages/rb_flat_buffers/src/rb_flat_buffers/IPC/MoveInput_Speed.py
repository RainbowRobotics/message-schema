# automatically generated by the FlatBuffers compiler, do not modify

# namespace: IPC

import flatbuffers
from flatbuffers.compat import import_numpy
from typing import Any
np = import_numpy()

class MoveInput_Speed(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset: int = 0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = MoveInput_Speed()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsMoveInput_Speed(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # MoveInput_Speed
    def Init(self, buf: bytes, pos: int):
        self._tab = flatbuffers.table.Table(buf, pos)

    # MoveInput_Speed
    def SpdMode(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # MoveInput_Speed
    def SpdVelPara(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 0.0

    # MoveInput_Speed
    def SpdAccPara(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 0.0

def MoveInput_SpeedStart(builder: flatbuffers.Builder):
    builder.StartObject(3)

def Start(builder: flatbuffers.Builder):
    MoveInput_SpeedStart(builder)

def MoveInput_SpeedAddSpdMode(builder: flatbuffers.Builder, spdMode: int):
    builder.PrependInt32Slot(0, spdMode, 0)

def AddSpdMode(builder: flatbuffers.Builder, spdMode: int):
    MoveInput_SpeedAddSpdMode(builder, spdMode)

def MoveInput_SpeedAddSpdVelPara(builder: flatbuffers.Builder, spdVelPara: float):
    builder.PrependFloat32Slot(1, spdVelPara, 0.0)

def AddSpdVelPara(builder: flatbuffers.Builder, spdVelPara: float):
    MoveInput_SpeedAddSpdVelPara(builder, spdVelPara)

def MoveInput_SpeedAddSpdAccPara(builder: flatbuffers.Builder, spdAccPara: float):
    builder.PrependFloat32Slot(2, spdAccPara, 0.0)

def AddSpdAccPara(builder: flatbuffers.Builder, spdAccPara: float):
    MoveInput_SpeedAddSpdAccPara(builder, spdAccPara)

def MoveInput_SpeedEnd(builder: flatbuffers.Builder) -> int:
    return builder.EndObject()

def End(builder: flatbuffers.Builder) -> int:
    return MoveInput_SpeedEnd(builder)


class MoveInput_SpeedT(object):

    # MoveInput_SpeedT
    def __init__(
        self,
        spdMode = 0,
        spdVelPara = 0.0,
        spdAccPara = 0.0,
    ):
        self.spdMode = spdMode  # type: int
        self.spdVelPara = spdVelPara  # type: float
        self.spdAccPara = spdAccPara  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        moveInputSpeed = MoveInput_Speed()
        moveInputSpeed.Init(buf, pos)
        return cls.InitFromObj(moveInputSpeed)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, moveInputSpeed):
        x = MoveInput_SpeedT()
        x._UnPack(moveInputSpeed)
        return x

    # MoveInput_SpeedT
    def _UnPack(self, moveInputSpeed):
        if moveInputSpeed is None:
            return
        self.spdMode = moveInputSpeed.SpdMode()
        self.spdVelPara = moveInputSpeed.SpdVelPara()
        self.spdAccPara = moveInputSpeed.SpdAccPara()

    # MoveInput_SpeedT
    def Pack(self, builder):
        MoveInput_SpeedStart(builder)
        MoveInput_SpeedAddSpdMode(builder, self.spdMode)
        MoveInput_SpeedAddSpdVelPara(builder, self.spdVelPara)
        MoveInput_SpeedAddSpdAccPara(builder, self.spdAccPara)
        moveInputSpeed = MoveInput_SpeedEnd(builder)
        return moveInputSpeed
