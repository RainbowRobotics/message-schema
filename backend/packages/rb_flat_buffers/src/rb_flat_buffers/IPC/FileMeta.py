# automatically generated by the FlatBuffers compiler, do not modify

# namespace: IPC

import flatbuffers
from flatbuffers.compat import import_numpy
from typing import Any
from typing import Optional
np = import_numpy()

class FileMeta(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset: int = 0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = FileMeta()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsFileMeta(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # FileMeta
    def Init(self, buf: bytes, pos: int):
        self._tab = flatbuffers.table.Table(buf, pos)

    # FileMeta
    def Session(self) -> Optional[str]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # FileMeta
    def Name(self) -> Optional[str]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # FileMeta
    def Size(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # FileMeta
    def Mime(self) -> Optional[str]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # FileMeta
    def ChunkSize(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # FileMeta
    def TotalChunks(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # FileMeta
    def Sha256(self, j: int):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # FileMeta
    def Sha256AsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # FileMeta
    def Sha256Length(self) -> int:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # FileMeta
    def Sha256IsNone(self) -> bool:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        return o == 0

def FileMetaStart(builder: flatbuffers.Builder):
    builder.StartObject(7)

def Start(builder: flatbuffers.Builder):
    FileMetaStart(builder)

def FileMetaAddSession(builder: flatbuffers.Builder, session: int):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(session), 0)

def AddSession(builder: flatbuffers.Builder, session: int):
    FileMetaAddSession(builder, session)

def FileMetaAddName(builder: flatbuffers.Builder, name: int):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(name), 0)

def AddName(builder: flatbuffers.Builder, name: int):
    FileMetaAddName(builder, name)

def FileMetaAddSize(builder: flatbuffers.Builder, size: int):
    builder.PrependUint64Slot(2, size, 0)

def AddSize(builder: flatbuffers.Builder, size: int):
    FileMetaAddSize(builder, size)

def FileMetaAddMime(builder: flatbuffers.Builder, mime: int):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(mime), 0)

def AddMime(builder: flatbuffers.Builder, mime: int):
    FileMetaAddMime(builder, mime)

def FileMetaAddChunkSize(builder: flatbuffers.Builder, chunkSize: int):
    builder.PrependUint32Slot(4, chunkSize, 0)

def AddChunkSize(builder: flatbuffers.Builder, chunkSize: int):
    FileMetaAddChunkSize(builder, chunkSize)

def FileMetaAddTotalChunks(builder: flatbuffers.Builder, totalChunks: int):
    builder.PrependUint32Slot(5, totalChunks, 0)

def AddTotalChunks(builder: flatbuffers.Builder, totalChunks: int):
    FileMetaAddTotalChunks(builder, totalChunks)

def FileMetaAddSha256(builder: flatbuffers.Builder, sha256: int):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(sha256), 0)

def AddSha256(builder: flatbuffers.Builder, sha256: int):
    FileMetaAddSha256(builder, sha256)

def FileMetaStartSha256Vector(builder, numElems: int) -> int:
    return builder.StartVector(1, numElems, 1)

def StartSha256Vector(builder, numElems: int) -> int:
    return FileMetaStartSha256Vector(builder, numElems)

def FileMetaEnd(builder: flatbuffers.Builder) -> int:
    return builder.EndObject()

def End(builder: flatbuffers.Builder) -> int:
    return FileMetaEnd(builder)

try:
    from typing import List
except:
    pass

class FileMetaT(object):

    # FileMetaT
    def __init__(self):
        self.session = None  # type: str
        self.name = None  # type: str
        self.size = 0  # type: int
        self.mime = None  # type: str
        self.chunkSize = 0  # type: int
        self.totalChunks = 0  # type: int
        self.sha256 = None  # type: List[int]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        fileMeta = FileMeta()
        fileMeta.Init(buf, pos)
        return cls.InitFromObj(fileMeta)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, fileMeta):
        x = FileMetaT()
        x._UnPack(fileMeta)
        return x

    # FileMetaT
    def _UnPack(self, fileMeta):
        if fileMeta is None:
            return
        self.session = fileMeta.Session()
        self.name = fileMeta.Name()
        self.size = fileMeta.Size()
        self.mime = fileMeta.Mime()
        self.chunkSize = fileMeta.ChunkSize()
        self.totalChunks = fileMeta.TotalChunks()
        if not fileMeta.Sha256IsNone():
            if np is None:
                self.sha256 = []
                for i in range(fileMeta.Sha256Length()):
                    self.sha256.append(fileMeta.Sha256(i))
            else:
                self.sha256 = fileMeta.Sha256AsNumpy()

    # FileMetaT
    def Pack(self, builder):
        if self.session is not None:
            session = builder.CreateString(self.session)
        if self.name is not None:
            name = builder.CreateString(self.name)
        if self.mime is not None:
            mime = builder.CreateString(self.mime)
        if self.sha256 is not None:
            if np is not None and type(self.sha256) is np.ndarray:
                sha256 = builder.CreateNumpyVector(self.sha256)
            else:
                FileMetaStartSha256Vector(builder, len(self.sha256))
                for i in reversed(range(len(self.sha256))):
                    builder.PrependUint8(self.sha256[i])
                sha256 = builder.EndVector()
        FileMetaStart(builder)
        if self.session is not None:
            FileMetaAddSession(builder, session)
        if self.name is not None:
            FileMetaAddName(builder, name)
        FileMetaAddSize(builder, self.size)
        if self.mime is not None:
            FileMetaAddMime(builder, mime)
        FileMetaAddChunkSize(builder, self.chunkSize)
        FileMetaAddTotalChunks(builder, self.totalChunks)
        if self.sha256 is not None:
            FileMetaAddSha256(builder, sha256)
        fileMeta = FileMetaEnd(builder)
        return fileMeta
