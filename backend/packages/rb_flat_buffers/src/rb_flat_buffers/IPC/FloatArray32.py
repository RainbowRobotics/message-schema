# automatically generated by the FlatBuffers compiler, do not modify

# namespace: IPC

import flatbuffers
from flatbuffers.compat import import_numpy
from typing import Any
np = import_numpy()

class FloatArray32(object):
    __slots__ = ['_tab']

    @classmethod
    def SizeOf(cls) -> int:
        return 128

    # FloatArray32
    def Init(self, buf: bytes, pos: int):
        self._tab = flatbuffers.table.Table(buf, pos)

    # FloatArray32
    def Arr(self, j = None):
        if j is None:
            return [self._tab.Get(flatbuffers.number_types.Float32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(0 + i * 4)) for i in range(self.ArrLength())]
        elif j >= 0 and j < self.ArrLength():
            return self._tab.Get(flatbuffers.number_types.Float32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(0 + j * 4))
        else:
            return None

    # FloatArray32
    def ArrAsNumpy(self):
        return self._tab.GetArrayAsNumpy(flatbuffers.number_types.Float32Flags, self._tab.Pos + 0, self.ArrLength())

    # FloatArray32
    def ArrLength(self) -> int:
        return 32

    # FloatArray32
    def ArrIsNone(self) -> bool:
        return False


def CreateFloatArray32(builder, arr):
    builder.Prep(4, 128)
    for _idx0 in range(32 , 0, -1):
        builder.PrependFloat32(arr[_idx0-1])
    return builder.Offset()

try:
    from typing import List
except:
    pass

class FloatArray32T(object):

    # FloatArray32T
    def __init__(
        self,
        arr = None,
    ):
        self.arr = arr  # type: Optional[List[float]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        floatArray32 = FloatArray32()
        floatArray32.Init(buf, pos)
        return cls.InitFromObj(floatArray32)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, floatArray32):
        x = FloatArray32T()
        x._UnPack(floatArray32)
        return x

    # FloatArray32T
    def _UnPack(self, floatArray32):
        if floatArray32 is None:
            return
        if not floatArray32.ArrIsNone():
            if np is None:
                self.arr = []
                for i in range(floatArray32.ArrLength()):
                    self.arr.append(floatArray32.Arr(i))
            else:
                self.arr = floatArray32.ArrAsNumpy()

    # FloatArray32T
    def Pack(self, builder):
        return CreateFloatArray32(builder, self.arr)
