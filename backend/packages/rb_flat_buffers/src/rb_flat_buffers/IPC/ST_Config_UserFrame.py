# automatically generated by the FlatBuffers compiler, do not modify

# namespace: IPC

import flatbuffers
from flatbuffers.compat import import_numpy
from typing import Any
from IPC.Vec3f import Vec3f
from typing import Optional
np = import_numpy()

class ST_Config_UserFrame(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset: int = 0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = ST_Config_UserFrame()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsST_Config_UserFrame(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # ST_Config_UserFrame
    def Init(self, buf: bytes, pos: int):
        self._tab = flatbuffers.table.Table(buf, pos)

    # ST_Config_UserFrame
    def UserfName(self) -> Optional[str]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # ST_Config_UserFrame
    def UserfOffset(self) -> Optional[Vec3f]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = o + self._tab.Pos
            obj = Vec3f()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # ST_Config_UserFrame
    def UserfEuler(self) -> Optional[Vec3f]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = o + self._tab.Pos
            obj = Vec3f()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def ST_Config_UserFrameStart(builder: flatbuffers.Builder):
    builder.StartObject(3)

def Start(builder: flatbuffers.Builder):
    ST_Config_UserFrameStart(builder)

def ST_Config_UserFrameAddUserfName(builder: flatbuffers.Builder, userfName: int):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(userfName), 0)

def AddUserfName(builder: flatbuffers.Builder, userfName: int):
    ST_Config_UserFrameAddUserfName(builder, userfName)

def ST_Config_UserFrameAddUserfOffset(builder: flatbuffers.Builder, userfOffset: Any):
    builder.PrependStructSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(userfOffset), 0)

def AddUserfOffset(builder: flatbuffers.Builder, userfOffset: Any):
    ST_Config_UserFrameAddUserfOffset(builder, userfOffset)

def ST_Config_UserFrameAddUserfEuler(builder: flatbuffers.Builder, userfEuler: Any):
    builder.PrependStructSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(userfEuler), 0)

def AddUserfEuler(builder: flatbuffers.Builder, userfEuler: Any):
    ST_Config_UserFrameAddUserfEuler(builder, userfEuler)

def ST_Config_UserFrameEnd(builder: flatbuffers.Builder) -> int:
    return builder.EndObject()

def End(builder: flatbuffers.Builder) -> int:
    return ST_Config_UserFrameEnd(builder)

try:
    from typing import Optional
except:
    pass

class ST_Config_UserFrameT(object):

    # ST_Config_UserFrameT
    def __init__(
        self,
        userfName = None,
        userfOffset = None,
        userfEuler = None,
    ):
        self.userfName = userfName  # type: Optional[str]
        self.userfOffset = userfOffset  # type: Optional[Vec3fT]
        self.userfEuler = userfEuler  # type: Optional[Vec3fT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        stConfigUserFrame = ST_Config_UserFrame()
        stConfigUserFrame.Init(buf, pos)
        return cls.InitFromObj(stConfigUserFrame)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, stConfigUserFrame):
        x = ST_Config_UserFrameT()
        x._UnPack(stConfigUserFrame)
        return x

    # ST_Config_UserFrameT
    def _UnPack(self, stConfigUserFrame):
        if stConfigUserFrame is None:
            return
        self.userfName = stConfigUserFrame.UserfName()
        if stConfigUserFrame.UserfOffset() is not None:
            self.userfOffset = Vec3fT.InitFromObj(stConfigUserFrame.UserfOffset())
        if stConfigUserFrame.UserfEuler() is not None:
            self.userfEuler = Vec3fT.InitFromObj(stConfigUserFrame.UserfEuler())

    # ST_Config_UserFrameT
    def Pack(self, builder):
        if self.userfName is not None:
            userfName = builder.CreateString(self.userfName)
        ST_Config_UserFrameStart(builder)
        if self.userfName is not None:
            ST_Config_UserFrameAddUserfName(builder, userfName)
        if self.userfOffset is not None:
            userfOffset = self.userfOffset.Pack(builder)
            ST_Config_UserFrameAddUserfOffset(builder, userfOffset)
        if self.userfEuler is not None:
            userfEuler = self.userfEuler.Pack(builder)
            ST_Config_UserFrameAddUserfEuler(builder, userfEuler)
        stConfigUserFrame = ST_Config_UserFrameEnd(builder)
        return stConfigUserFrame
