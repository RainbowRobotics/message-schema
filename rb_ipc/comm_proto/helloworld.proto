syntax = "proto3";

package helloworld;

// 인사말 서비스를 정의합니다.
service Greeter {
  // 'SayHello'라는 원격 함수를 정의합니다.
  rpc SayHello (HelloRequest) returns (HelloReply);
}

// 요청 메시지 형식입니다.
message HelloRequest {
  string name = 1;
}

// 응답 메시지 형식입니다.
message HelloReply {
  string message = 1;
}
