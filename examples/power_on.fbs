include "error_code.fbs";

namespace rb.v1;

enum PowerState : ubyte { Unknown = 0, Pending = 1, On = 2, Off = 3, Error = 4 }

table PowerOnRequest {
  id: string;
}

table PowerOnResponse {
  id: string;
  state: PowerState;
  error: ErrorCode;
}